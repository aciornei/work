BACKUP_DIR=$(sudo ls -dt /opt/vm-doc-server/backup/* | head -1)

sudo mariadb-dump \
  -u vm_doc \
  -p \
  --single-transaction \
  --quick \
  --routines \
  --triggers \
  --events \
  --hex-blob \
  --default-character-set=utf8mb4 \
  vm_doc \
  | sudo gzip -9 > "$BACKUP_DIR/vm_doc.sql.gz"
