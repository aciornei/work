sudo mariadb vm_doc <<'SQL'
ALTER TABLE vm_internal_service_assignments
  ADD UNIQUE INDEX IF NOT EXISTS uq_vm_service_context_assignment
  (
    vm_id,
    architectural_domain_id,
    itil_category_id,
    internal_service_id,
    layer_id,
    technical_role_id
  );
SQL
